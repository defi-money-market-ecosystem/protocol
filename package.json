{
  "name": "dmm-protocol",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "oz compile",
    "format": "solium -d contracts/",
    "start-chain": "ganache-cli --deterministic --port 8545",
    "test": "oz compile && npx mocha --exit --recursive test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/cli": "^2.6.0",
    "@openzeppelin/contract-loader": "^0.6.1",
    "@openzeppelin/contracts": "^2.4.0",
    "ganache-cli": "^6.9.0"
  },
  "devDependencies": {
    "@openzeppelin/test-environment": "^0.1.2",
    "@openzeppelin/test-helpers": "^0.5.4",
    "chai": "^4.2.0",
    "mocha": "^7.0.1"
  }
}
